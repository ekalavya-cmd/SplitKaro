{
  "name": "splitKaro-root",
  "private": true,
  "description": "Root package strictly for managing project-level development tooling (e.g. husky, commitlint).",
  "devDependencies": {
    "@commitlint/cli": "^21.2.2",
    "@commitlint/config-conventional": "^21.2.2",
    "husky": "^9.1.7"
  },
  "scripts": {
    "prepare": "husky"
  }
}
